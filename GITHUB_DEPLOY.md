# GitHub 上線說明

這個 ZIP 已包含原始碼、設定、GitHub Actions、Netlify 設定與一份離線示範輸出。
解壓縮後，請把**資料夾內的檔案本身**放到 GitHub repository 根目錄，不要再多包一層資料夾。

## 第一次上線

1. 在 GitHub 建立新的 repository，將解壓縮後的全部檔案上傳到 `main` 分支。
2. 到 `Settings → Actions → General → Workflow permissions`，選擇 `Read and write permissions`。
3. 到 `Settings → Secrets and variables → Actions → Secrets` 新增：
   - `FRED_API_KEY`：必填，正式抓取 FRED／ALFRED 資料。
   - `BLS_API_KEY`：選填；不設仍可使用 BLS v1，但額度較低。
4. 到同一頁的 `Variables` 新增：
   - `SEC_USER_AGENT`：建議必填，格式為 `你的名稱 your@email.com`。
5. 到 `Actions → 更新儀表板 → Run workflow` 手動執行一次。
6. 執行成功後，GitHub Actions 會把正式頁面寫回 `output/`，並把比較基準寫回 `state/`。
7. 在 Netlify 選 `Add new site → Import an existing project`，連結這個 repository。
   `netlify.toml` 已設定發布目錄為 `output`，不需要額外填 build command。

## 之後如何更新

- 平日美國上午自動跑三次，週末一次。
- 修改程式或設定並 push 後，也會立即重建。
- 資料沒有變化時不會產生空 commit。
- 時間序列若出現日期錯序、重複、空值或未來實際值，建置會停止，不發布錯誤頁面。

## 本次 1–10 改版內容

1. **總覽首卡**：九宮格位置、兩軸方向、FOMC、長端供給與下一格門檻同屏。
2. **卡片層級**：每頁先給決策首卡，其餘集中到一個「完整分析」；不再多層深藏。
3. **用詞統一**：格位只用強／中／弱與高／中／低；方向只用轉強／持平／轉弱與升溫／持平／降溫。
4. **九宮格邏輯**：當前位置與移動方向分開；FOMC 反應體制只改政策解讀，不改兩軸資料。
5. **CPI／PPI／PCE**：總體與核心口徑完整接入，並建立 PPI → CPI → PCE 傳導鏈。
6. **就業頁**：失業率決定格位；非農、損益兩平、失業金與 JOLTS 決定方向。
7. **FOMC 頁**：會議決定、投票方向、客觀分數、文本比對與下次會議集中在首卡。
8. **財政與 Hyperscalers**：財政赤字、穩定差、營運現金流、CapEx 與發債放在同一供給尺度。
9. **資料治理**：加入發布前時間序列完整性閘門，並停用未接上分析的多餘序列。
10. **UI／QA**：完成 360、390、760、1280px 版面稽核、WCAG AA 對比與全套邏輯測試。

## 九宮格的固定規則

- 就業格位：失業率相對 FOMC 長期失業率中央趨勢；Sahm 法則觸發是唯一緊急覆寫。
- 就業方向：近三個月平均非農相對損益兩平；其他就業指標用作交叉確認，不直接移格。
- 通膨格位：核心 PCE 年增相對動態門檻。
- 通膨方向：核心 PCE、核心 CPI、核心 PPI 的三個月年化相對年增，至少兩者同向才確認。
- PPI：只代表上游壓力，不直接移動九宮格。
- FOMC：只決定使用通膨優先、就業優先或兩邊並重的政策反應表。
- 財政、Hyperscalers 與期限溢酬：是九宮格外的長端覆蓋層，只影響曲線形狀。

## 通膨口徑

| 指標 | 總體 | 核心 | 在網站的角色 |
|---|---|---|---|
| CPI | 含食物與能源 | 剔除食物與能源 | 最早確認消費端轉折與分項來源 |
| PPI | 最終需求總指數 | 最終需求剔除食物與能源；仍含貿易服務 | 企業端上游成本／售價壓力 |
| PCE | 含食物與能源 | 剔除食物與能源 | 總體 PCE 對應 Fed 2% 長期目標；核心 PCE 看基礎趨勢並決定格位 |

## 上線後先檢查

- Actions 最新一次執行為綠色。
- 首頁顯示的資料月份符合最近一次正式發布。
- 通膨首卡的 CPI、PPI、PCE 各自顯示自己的資料月份。
- FOMC 比對的是兩份正式會議聲明，不含非會議策略文件。
- Hyperscalers 區塊顯示 `SEC 實際資料 N/5 家`；若不是 5/5，先檢查 `SEC_USER_AGENT`。
