# 日常操作手冊

一句話：**平常什麼都不用做。** 每天晚上 21:45（台灣時間，＝13:45 UTC）系統自己更新，
你只要在數據發布後看網站就好。

真正需要你動手的只有四件事：兩件一季一次、一件一年一次，
剩下一件是選配。（政策利率區間本來也要手動，現在已經自動抓了。）

---

## 完全自動（你不用碰）

每天 13:45 UTC 由 GitHub Actions 執行一次，抓完資料重產整站並自動部署。

| 內容 | 來源 | 更新頻率 |
|---|---|---|
| 就業、失業率、時薪、參與率、JOLTS、失業金 | FRED（BLS／DOL） | 資料一發布就跟上 |
| CPI、PCE、分項貢獻、通膨預期、油價 | FRED（BLS／BEA／Cleveland Fed） | 同上 |
| 殖利率曲線、期限溢酬、信用利差、政府債務 | FRED（財政部／聯準會） | 同上 |
| **聯準會聲明全文、投票紀錄、反對票方向** | federalreserve.gov | 會後當天 |
| **記者會逐字稿與主題摘要** | federalreserve.gov（PDF） | 會後數日 |
| **科技巨頭資本支出／營運現金流／發債** | SEC EDGAR XBRL | 財報一申報就跟上（季末後約 45 天） |
| **科技巨頭的新發債申報** | SEC EDGAR 申報清單（424B2／424B5／8-K 2.03） | 定價當天到四個營業日內 |
| **科技巨頭的財報公布日** | SEC EDGAR 申報清單（8-K 項目 2.02） | 新聞稿一申報就跟上（季末後約 3 週） |
| 修正追蹤、行業歸因、規則引擎、九宮格、反應函數 | 上述資料計算而得 | 每次執行 |

排程時間刻意設在 13:45 UTC：美東夏令是 09:45、冬令是 08:45，
兩者都在 08:30 ET 的資料發布之後，**所以換季不必調整**。

非發布日跑起來只是重新產生同一份頁面，沒有副作用。

---

## 需要你手動維護的四件事

### ① 市場預期 — 每次數據發布前（唯一「常態」工作）

**檔案**：`config/consensus.yaml`　**目前狀態**：空的

這是唯一沒有免費資料源的東西。沒填的話，「實際 vs 市場預期」那一區會
退回時間序列模型推估，並在畫面上明確標示「這不是市場預期」。

想要真正的意外值判讀，就在就業報告／CPI 發布**前**填入券商調查的中位數：

```yaml
expectations:
  "2026-09":
    PAYEMS: 85        # 非農月變動，單位千人（+8.5 萬 → 填 85）
    UNRATE: 4.2       # 失業率 %
    CPIAUCSL_YOY: 3.2 # 總體 CPI 年增率 %
    CPILFESL_YOY: 3.1 # 核心 CPI 年增率 %
```

鍵名必須完全相符，程式是照字面去查的；打錯只會靜靜地退回模型推估，
不會報錯。以 `config/consensus.yaml` 檔頭那份清單為準——
通膨兩項收的是**年增率**（`_YOY`），不是月增率。

不填不會壞，只是少一個判讀維度。**如果你不做這件事，這套系統就是全自動的。**

### ② 投資級季度發行量 — 一季一次

**檔案**：`config/rates.yaml` 的 `ig_market.quarterly_issuance`
**來源**：SIFMA 季度投資級公司債發行統計
**用途**：算「科技巨頭發債佔投資級市場多少比重」

只影響長端頁的一個比率，不更新不會讓其他部分失真。

### ③ 科技巨頭的資本支出指引 — 一季一次（法說會後）

**檔案**：`config/rates.yaml` 的 `capex_guidance`
**來源**：各公司法說會與財報新聞稿（8-K 的 EX-99.1）
**用途**：長端頁「科技巨頭」卡最上方的「N 年資本支出計畫」

**為什麼這一項不能自動**：前瞻指引不在任何申報欄位裡。它是法說會上
用自然語言講的——「approximately $190 billion」「in the range of
$125 to $145 billion」「slightly above last year」——每家寫法不同、
每季還會改。硬解析的失敗方式是安靜地少一家，或抓到錯的口徑。

**為什麼值得手動**：SEC 的 XBRL 只有**已經花掉的**上一季，而這一頁的論點是
「AI 資本支出把科技巨頭從淨買方變成淨賣方、推高長端供給」——
那個故事的主角是**接下來要花多少**。四個數字、一年更新四次、每次兩分鐘。

怎麼填（單位十億美元，區間填 low／high，單一數字就兩邊填一樣）：

```yaml
capex_guidance:
  enabled: true
  year: 2026
  as_of: "2026-04-29"        # 最後一次更新的日期，畫面上會標出來
  companies:
    - {name: Microsoft, low: 190.0, high: 190.0}
    - {name: Alphabet,  low: 175.0, high: 185.0}
    - {name: Oracle,    low: 0.0,   high: 0.0, note: 未提供年度指引}
```

填 0 的公司**不會**被算成零併進合計，而是在畫面上單獨標示「未提供年度指引，
不在合計內」。整段刪掉或設 `enabled: false` 時這一區直接不顯示，不會壞。

⚠️ `as_of` 一定要跟著改——畫面上就是靠它讓讀者知道這批數字有多舊。

### ④ CPI 相對重要性權重 — 一年一次（每年一月）

**檔案**：`config/inflation.yaml` 各分項的 `weight`
**來源**：BLS relative importance 表

⚠️ 這個最容易忘記，而且**不更新會讓分項貢獻度默默失真**——
權重乘上變化率就是貢獻度，權重錯了整個歸因就錯了，但畫面上看不出來。
建議在行事曆設一個一月的提醒。

### ⑤ 政策利率區間 — **已改成自動，你不用碰了**

現在直接抓 FRED 的 `DFEDTARL`／`DFEDTARU`，聯準會一動就跟上。
`config/fomc.yaml` 的 `policy_rate` 降級成**抓不到時的後備值**。

之所以優先自動化這一項：它先前被描述成「只用於畫面顯示」，但實際上
FOMC 頁的「市場定價 vs 聯準會」是拿 `2 年期殖利率 − 政策利率中值` 去判定的，
門檻只有 ±0.15 個百分點。漏更新一次降息（25 個基點）會讓判定平移 0.25，
足以把「市場偏降息」翻成「偏向再緊縮」——**而且畫面上完全看不出來**。
這是整份專案裡唯一一個「漏更新會默默給出反向結論」的手動項。

真的抓不到而退回後備值時，KPI 卡上會直接寫出來，不會靜靜地用舊值。

---

## 建議設定一次就好：SEC_USER_AGENT

**位置**：repo → Settings → Secrets and variables → Actions → **Variables** 分頁
→ New repository variable
**Name**：`SEC_USER_AGENT`　**Value**：`你的名字 你的信箱`（例如 `GC gc@example.com`）

SEC 規定抓取它的資料要帶可聯絡的 User-Agent。沒設定的話程式會用一個
內建的預設值，格式合法但信箱是假的——SEC 有可能限流甚至擋掉。

**擋掉的後果值得特別講**：科技巨頭那一區會退回 `config/rates.yaml` 的
手動後備值，而畫面上仍然有數字、看起來完全正常，只是那是上一次人工填的
舊數字。這種失敗曾經真的發生過（見 README 的「安靜地用了舊數字」）。
現在退回時執行紀錄會印一行 error，畫面上也會直接寫「這一區的數字全部
不是最新的」，但最好的做法還是把這個變數設好。

---

## 改設定怎麼做

全程在 GitHub 網頁上完成，不用裝任何東西：

1. repo → 進 `config/` → 點檔案
2. 右上角鉛筆 **Edit this file**
3. 改完 → 下方 **Commit changes**

下次排程執行就會套用。想立刻看到結果：**Actions** → **更新儀表板**
→ **Run workflow**，兩三分鐘後 Netlify 自動部署。

---

## 出事時怎麼看

**網站沒更新** → 按這個順序查，兩分鐘可以定位到是哪一段斷掉：

1. **看網站頁面上方的「更新於 YYYY-MM-DD HH:MM（台北）」**。那是頁面產生的時間，
   不是資料月份。它如果還是舊的，就代表新的產出沒有上線——繼續往下查。
   排程跑完的話這個時間會落在**晚上 21:4x**；出現別的時間就代表那次是
   push 或手動觸發的。
2. **看 GitHub 上的 `output/index.html` 最後一次 commit 是什麼時候。**
   - GitHub 上是新的、網站是舊的 → **Netlify 那一段**的問題（跳到第 5 點）
   - GitHub 上也是舊的 → **git 或 Actions** 的問題（第 3、4 點）
3. **確認 `output/` 真的有進 git。** 在本機跑
   `git check-ignore -v output/index.html`——有任何輸出就代表它被忽略了。
   這是最常見的一種：repo 早期建立時 `.gitignore` 裡有 `output/`，
   之後就算改掉 `.gitignore`，那些**從來沒被追蹤過**的檔案還是不會被 `git add .` 收進去。
   解法：`git add -f output && git commit -m "加入產出" && git push`。
4. **Actions 分頁看最新那筆是不是綠勾。** 紅叉點進去看哪一步掛的：
   - 掛在「產生頁面」→ 多半是 `FRED_API_KEY` 沒設或已失效
   - 掛在「Commit 產出」→ 多半是分支被保護，或 repo 的 Actions 沒有寫入權限
     （Settings → Actions → General → Workflow permissions 要選 Read and write）
   - 顯示「資料沒有變化，跳過 commit」→ 那是正常的：非發布日重跑會產出同一份頁面
5. **Netlify → Deploys** 看有沒有對應時間的部署。沒有的話到
   Site configuration → Build & deploy 確認：連的是同一個 repo、分支對、
   Publish directory 是 `output`、Build command 留空、而且沒有把自動部署暫停。

**改了程式推上去，但網站沒變** → 排程只在每天 13:45 UTC 跑一次。
現在 `push` 也會觸發（改到 `output/`、`state/` 與 `.md` 以外的檔案時），
所以推上去幾分鐘內就會重產。想立刻重跑：Actions → 更新儀表板 → Run workflow。

**某些數字變成「—」** → 正常。FRED 偶爾改序列代號或暫時抓不到，
系統是逐序列容錯的，單一序列失敗不會讓整份報告產不出來。
勞動市場頁的底部會列出當次失敗的序列；其餘分頁只記錄在 GitHub Actions 的執行紀錄裡。

**科技巨頭那區出現「部分數字未取自 SEC」** → SEC 抓取失敗，該公司退回
`config/rates.yaml` 的後備值。偶爾一次通常是 SEC 限流，下次執行就會恢復；
持續出現再看失敗清單。

**失業率變動分解底下出現「方向可看，強度別當真」** → 這是新加的顯著性檢查。
失業率只公布到小數一位，而 BLS 自己的標準是月變動要 **0.2 個百分點**才在
90% 信賴水準下顯著。低於這個值時，那條訊號會從 alert 降成 info——先前一個
統計上不可分辨於零的 0.1 個百分點，會產出 alert 級、權重 2.0 的結論，
兩條就能翻動九宮格的列。

**「兩項相加」那一行後面有時會多一個「近似誤差」** → 正常。分解式
Δu ≈ −ΔE/L + (E/L)(ΔL/L) 是一階近似，而且總變動取自四捨五入到小數一位的
失業率、兩個效果取自未四捨五入的家庭調查數字，本來就不會完全相等。
誤差大到 0.03 個百分點以上才會標出來，免得那一行被當成算錯。

**發債交易列出「金額待確認」** → 正常。金額是從說明書封面用文字比對解析的，
封面格式偶爾不合預期就會讀不到。系統**不會**用推估值填補，也不把這筆計入合計——
點該列右邊的原文連結可以直接看 SEC 的申報書。下次執行仍然讀不到就是格式問題，
不影響其他數字。

**發債交易列出「尚未定價」** → 也是正常，而且跟上面那條不一樣。它代表那份
文件是**預估版說明書**（封面寫著 Subject to Completion），定價欄本來就是空的，
不是解析失敗。這種通常兩三天後會出現定價版，屆時它會自動被合併掉、
換成有金額的那一筆。預估版**不計入筆數也不計入合計**。

**發債合計金額整個不見了，只剩筆數** → 這是刻意的保險。合計超過最新一季
季報申報值的 3 倍時，系統判定解析結果不可信，寧可不報金額。畫面上會直接
說明原因。這一區的價值在時效（比季報早三個月看到有幾筆新發債），
沒有金額仍然成立；報一個錯了四倍的合計則會毀掉整頁的可信度。
偶爾一次多半是某份封面的格式特殊，持續出現就要看解析規則是不是要調整。

**「跟上期比」顯示的是好幾天前的事** → 這是刻意的。它比的是**每個模組
自己的上一期發布**，不是上一次執行——排程每天跑，但資料一個月才出一次。
所以「7 月就業報告帶來的變化」會一直留到 8 月報告出來為止，卡片上會標
「6 天前發布」讓你知道這不是今天的新聞。這樣不管哪一天打開網站，
都看得到最近一次真正的變化，而不是只有發布當天那 24 小時才看得到。

**「跟上期比」空白** → 只有兩種情況：第一次執行（還沒有可比對的上期），
或是兩期資料之間訊號組成真的完全沒變。後者少見但會發生，
畫面會直接說「情境與訊號組成與上期相同」。

**想知道為什麼就業是「弱」、通膨是「高」** → 九宮格正下方有兩條列，
一條軸一條，直接寫出理由與關鍵數字（例如「失業率 4.1% 仍在充分就業區間，
但三月均非農 +2.0 萬低於損益兩平 +4.3 萬」）。點該列右邊的箭頭會展開完整算式：
每一項的權重、加總結果、門檻是多少、**門檻的出處**（通膨軸與就業軸都錨在
FOMC 自己的預測，不是本站選的數字）。

**通膨頁說「本期方向不明」，九宮格卻把通膨歸在「高」** → 這不是矛盾，兩者
問的不是同一件事：模組頁的結論看的是**本期新訊號的方向**（旗標鷹鴿淨值），
九宮格的軸看的是**水準**（核心 PCE 離 2% 目標多遠）。卡在高檔但這個月沒有
新的推力，兩者可以同時成立。九宮格下方的「這一格是怎麼判出來的」會在兩者
看起來打架時直接解釋，並把算式與門檻印出來讓你驗算。

**九宮格的通膨門檻會自己變** → 對，那是刻意的。門檻錨在聯準會自己的預測：
低＝長期目標＋0.25、高＝FOMC 對明年的核心 PCE 預測中位數（FRED 的
`PCECTPIMDLR` 與 `JCXFEMD`）。SEP 一季更新一次，所以門檻一年會動幾次，
畫面上會寫出用的是哪一年的預測。抓不到就退回 2.30／2.90 並標示
「沒有外部依據」。就業軸同樣錨在 SEP：水準看失業率相對 FOMC 長期失業率的中央趨勢
（`UNRATECTLLR`／`UNRATECTHLR`），再加兩條動能條件——Sahm 法則觸發直接判「弱」、
三月均非農低於損益兩平就往「弱」推一格。三條依據都是外部標準，
沒有一個是自訂數字。抓不到 SEP 才退回舊的 ±0.45／±3，畫面上會標示。

**首頁的「整體情勢」那一段是誰寫的** → 骨架永遠是程式組的：五個模組各出一句，
從**已經算好的判定**取值，資料一更新（月頻的就業與 CPI、會後的聲明、
季頻的 SEC 財報、每天的殖利率）這段話就跟著變；結尾固定收一句重點
（升降息的可能性與解鎖條件）。

設了 `ANTHROPIC_API_KEY` 的話，會再讓模型把這段**改寫得通順**——但模型
只能動措辭：輸出裡的每個數字都必須出現在原文（機械檢查，過不了就用原版）、
事實沒變不重新生成（快取在 `state/brief.json`，費用以分計）、
API 掛了自動退回原版。所有判斷仍然是確定性規則，模型一個數字都改不了。

**想開模型潤稿** → repo → Settings → Secrets and variables → Actions →
New repository secret，二選一（都設的話優先用 Gemini，因為有免費額度）：

| Secret 名稱 | 金鑰哪裡拿 | 用的模型 |
|---|---|---|
| `GEMINI_API_KEY` | aistudio.google.com/apikey（免費） | gemini-2.5-flash |
| `ANTHROPIC_API_KEY` | console.anthropic.com | claude-haiku-4-5 |

設完下次執行自動生效；不設就永遠用組裝版，什麼都不會壞。
執行紀錄會寫「整體情勢：模型潤稿，gemini-2.5-flash／規則組裝」，看得出走哪條路。
Gemini 免費層有每日次數限制，但這裡**事實沒變不會重新呼叫**，
一個月大約只打十幾次，離上限很遠。

⚠️ 金鑰**只能放 Secrets**：不要貼進聊天、不要寫進程式或 config、
不要 commit。貼出去過的金鑰一律視同外流，回到發金鑰的網站刪掉重生一把。

**分頁標題可以點** → 每一頁的大標題就是「回首頁」。手機上主選單有三分之一
在視窗外，在長端與情境頁上「總覽」會被捲到左邊看不見——點站名回首頁是通用慣例，
補這一條比較保險。選單兩側現在也有漸層，看得出還有沒有東西可以滑。

**頁面上方多了一列灰色的段落連結** → 那是頁內導覽，**平常不出現**，
展開三個卡區（或按「展開全部」）之後才淡入。理由是收合狀態下整頁只有兩三千 px，
卡片標題列自己就是目錄；按了展開全部會漲到一萬多 px，那時候才需要它。
出現之後就一直留著，捲動時會標出你現在在哪一段，手機上會自動把高亮那項捲進視野。
它跟最上方的分頁選單不一樣：**選單是換頁，這一列是換段**，
所以刻意做成純文字而不是按鈕。名詞解釋與判讀說明不列在上面。

**首頁也可以收合了** → 首頁先前是全站預設最長的頁（390px 下 4026px），
卻是唯一沒有收合的頁——整站的閱讀模型在入口那一頁不成立。現在跟內容頁一樣：
只留「本期關鍵訊號」展開，「接下來要盯什麼」與「跟上期比」收合並在標題列帶結論。
模組入口那四張卡也往上移到結論卡正下方（先前排在整頁最後，手機上要捲過
4000px 才看得到）。

**結論卡底部多了一條「這個判斷怎麼來的、對應什麼部位」** → 那是通往情境合成頁的
出口，直接跳到「固定收益部位對照」那張卡並自動展開。加這條是因為首頁 body 內
先前通往情境頁的連結是 **0 條**，只剩導覽列上那四個字——而讀者剛在首頁看過同一段
結論，理性推論是「我已經看過了」，於是全站唯一寫著「該怎麼擺部位」的那張表
沒有人會走到。

**結論卡上的「這一格已維持 N 期」** → 要累積兩期以上才會出現，所以剛開始會是空的。
它讀的是 `state/snapshot.json` 裡新增的 `history`：每換一期資料就記一列
（不到 100 bytes），同一期別重跑只會就地更新、不會多塞。這段歷史
**沒有辦法事後回填**，所以是越早開始累積越好。

**卡區大部分是收合的** → 這是刻意的。每一頁只留最上方的結論與第一張
內容卡展開，其餘收合並在標題列帶一句結論，你掃過去就知道哪幾張要點開。
想一次看完按頁面上方的「展開全部」；列印或存 PDF 時會自動全部展開。
要注意的是**收合的內容瀏覽器搜尋（Ctrl+F）找不到**——要搜字先按「展開全部」。

**發債交易的筆數看起來偏少** → 這是刻意的。只收 424B2／424B5（真正的債券發行
說明書）與配不到說明書的 8-K 2.03；FWP（行銷條款表，一筆債會發好幾份）、
424B3（再售登記）、8-K 1.01（供應合約那類的萬用項）都不算發行。
同一筆債的 424B 與 8-K 也會合併成一筆。詳見 README 的「季報之外另外追發債交易」。

**記者會顯示「尚未發布」** → 正常，逐字稿是會後數日才上線，之後會自動補上。
若顯示「環境缺少 PDF 解析套件」才是真的要處理（`requirements.txt` 少了 pdfplumber）。

**每一頁頂端出現「有資料序列停止更新」** → 這條**要處理**。它的意思是
FRED 抓得到那條序列、但最後一筆已經舊到不正常（月頻超過 75 天、
週頻 21 天、日頻 12 天，都已經把正常的發布時差算進去了）。
常見原因有三：政府停擺、來源機關改版、FRED 換了 series id。
先到 fred.stlouisfed.org 直接查那個代號——查得到但沒更新就是等；
查不到就是代號變了，要改 `config/*.yaml`。
警告條列的是「哪一條、最後一筆是哪天、幾天沒動」，頁面上的數字仍然
照最後一筆算，所以在恢復之前不要引用。

**「更新於」是什麼時候的時間** → 是 `run.py` **跑完那一刻**的時間，
跟資料月份無關，也跟 Netlify 部署完成的時間無關。三種情況會產生新的時間戳：

| 觸發 | 你要做什麼 | 時間戳長什麼樣 |
|---|---|---|
| 每天的排程 | 什麼都不用做 | 晚上 **21:4x** |
| 你 push 程式或改 config | 在 GitHub 上編輯並 commit | 你動手的時間 |
| Actions → Run workflow | 手動按 | 你按的時間 |

畫面上顯示的一律是**台北時間**並標「（台北）」。這件事要標清楚是因為
GitHub Actions 的機器時鐘是 UTC——不轉換的話排程跑完會印「13:46」，
跟這份文件寫的「晚上 21:45 更新」差 8 小時，看起來像壞掉。
`git log` 的 commit 訊息也一併轉成台北時間，兩邊才對得起來。

**倒數的日期怪怪的** → 就業報告與 CPI 的倒數優先用 FRED 的官方發布行事曆
（會標「非推估」）；問不到才退回「次月第一個週五」「次月第 12 天前後」的
慣例推估（會標「慣例推估」）。看到「慣例推估」而日期又不對，
通常是 BLS 遇到聯邦假日挪動了——下次執行問到官方行事曆就會自己修正。

---

## 選配：SEC 的 User-Agent（建議設一下）

SEC 的使用規範要求擷取資料時帶上可聯絡的 User-Agent。沒設會用預設值，
通常也能過，但被限流的機率較高（限流時科技巨頭那區會退回後備值）。

GitHub repo → **Settings** → **Secrets and variables** → **Actions**
→ **Variables** → **New repository variable**，
名稱 `SEC_USER_AGENT`，值填 `你的名字 你的信箱`（例如 `GC gc@example.com`）。

一樣是設一次就不用再碰。

---

## 選配：讓離線預覽也用真實資料

平常用不到，但如果你想在本機預覽真實數字（而不是示範序列）：

```bash
python run.py --save-fixtures    # 需要 FRED key
```

會把當次抓到的真實資料存成 `fixtures/*.json`，之後 `python run.py --offline`
就會讀這些快照。把 `fixtures/` commit 進 repo 的話，任何人 clone 下來
不用 key 也能看到真實數據。

---

## 年度行事曆速查

| 時間 | 要做什麼 |
|---|---|
| 每次數據發布前 | 填 `consensus.yaml` 的市場預期（選配） |
| 每季 | 更新 SIFMA 投資級發行量 |
| **每年一月** | **更新 CPI 相對重要性權重（最重要，別忘）** |
| 其他時候 | 什麼都不用做 |

> SEC User-Agent 是**一次性設定**，設完就不用再碰，見上方選配章節。
